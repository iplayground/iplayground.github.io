{
  "enabled": false,
  "name": "Changelog & Push (disabled)",
  "description": "（已停用）原流程會更新 CHANGELOG.md；此專案已移除 CHANGELOG.md。",
  "version": "1",
  "when": {
    "type": "userTriggered"
  },
  "then": {
    "type": "askAgent",
    "prompt": "此 hook 已停用：專案已移除 CHANGELOG.md。\n\n你可以改用一般提交流程：\n1. 執行 `git diff HEAD --stat` 與 `git status --short`\n2. 整理摘要（最多 10 點）\n3. `git add -A`\n4. 用繁體中文撰寫 commit message（標題 50 字以內，空一行後列點摘要）\n5. `git push`"
  }
}